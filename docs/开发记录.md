# 开发记录

## 说明

用于记录本项目每次功能开发、修复、配置调整和文档更新，方便后续排查问题和持续维护。

## 维护规则

- 每次完成代码、配置、数据库、模板或接口变更后，先更新本文档，再进行提交或交付。
- 如果本次改动影响接口、页面流程、字段含义、数据库结构或业务规则，也要同步更新相关设计、规则或说明文档。
- 记录按时间倒序追加，保持简洁、客观、可追溯。
- 只记录事实，不写主观评价。

## 记录模板

### YYYY-MM-DD | 类型
- 变更范围：
- 主要内容：
- 影响模块：
- 验证结果：
- 备注：

## 记录列表

### 2026-04-07 | 经营页移动端分段与分页
- 变更范围：`src/main/java/com/codexzy/service/BusinessRecordService.java`、`src/main/java/com/codexzy/service/impl/BusinessRecordServiceImpl.java`、`src/main/java/com/codexzy/controller/BusinessController.java`、`src/main/resources/templates/business/index.html`、`src/main/resources/templates/business/reporter.html`、`src/main/resources/static/css/app.css`、`docs/开发记录.md`
- 主要内容：为经营首页和报单人详情页补充经营记录分页；同时将手机端内容改成“总览 / 记录 / 筛选”三段切换，避免经营模块在小屏下持续整页下滑；删除记录和备注保存会尽量保留当前筛选、页码和移动端视图。
- 影响模块：经营后台、报单人详情、经营记录查询、移动端前端样式
- 验证结果：已使用 `C:\\Users\\admin\\.jdks\\ms-17.0.18` 执行 `mvn -q -DskipTests compile` 通过
- 备注：未改动数据库结构和现有业务字段，仅增加经营页内部展示参数 `page`、`view`。

### 2026-04-07 | Linux 重启脚本简化
- 变更范围：`restart-codexzy.sh`、`docs/上线部署指南.md`、`docs/开发记录.md`
- 主要内容：将 Linux 重启脚本收敛为“脚本与 Jar 同目录、直接 `nohup java -jar` 启动”的简单用法，移除环境变量覆盖、独立日志目录和启动等待等额外配置；同步更新部署文档说明。
- 影响模块：部署脚本、上线部署文档
- 验证结果：已使用 `C:\Users\admin\.jdks\ms-17.0.18` 执行 `mvn -q -DskipTests compile` 通过；当前环境未安装可用 Bash，未执行 `bash -n restart-codexzy.sh`
- 备注：脚本默认将日志写入同目录 `app.log`，PID 写入同目录 `app.pid`。

### 2026-04-07 | 报单目标多账号绑定
- 变更范围：`src/main/java/com/codexzy/dto/BusinessRecordFormDTO.java`、`src/main/java/com/codexzy/dto/BusinessReportTargetOptionDTO.java`、`src/main/java/com/codexzy/entity/BusinessReportTarget.java`、`src/main/java/com/codexzy/mapper/BusinessReportTargetMapper.java`、`src/main/java/com/codexzy/service/BusinessReportTargetService.java`、`src/main/java/com/codexzy/service/impl/BusinessReportTargetServiceImpl.java`、`src/main/java/com/codexzy/service/impl/BusinessRecordServiceImpl.java`、`src/main/java/com/codexzy/controller/BusinessController.java`、`src/main/resources/templates/business/form.html`、`src/main/resources/static/js/business-form.js`、`src/main/resources/schema.sql`、`src/test/java/com/codexzy/service/impl/BusinessRecordServiceImplTest.java`、`docs/开发记录.md`
- 主要内容：新增“常用报单目标”绑定表，支持一个账号保存多个常用报单对象；在报单表单中增加已绑定账号选择和“本次保存为常用账号”选项，并把最近使用的账号默认置顶和默认选中，减少重复填写报码。
- 影响模块：经营录入页、报单创建流程、数据库初始化、经营模块单元测试
- 验证结果：`mvn -q -DskipTests compile` 通过。
- 备注：未修改现有经营记录返回结构；新增表 `business_report_target` 需随 `schema.sql` 一起初始化。

### 2026-04-03 | 手机端模块导航收敛
- 变更范围：`src/main/resources/templates/fragments/mobile-tabbar.html`、`src/main/resources/templates/index.html`、`src/main/resources/templates/business/index.html`、`src/main/resources/templates/business/form.html`、`src/main/resources/templates/business/reporter.html`、`src/main/resources/templates/checkin/list.html`、`src/main/resources/templates/checkin/form.html`、`src/main/resources/templates/checkin/calendar.html`、`src/main/resources/templates/checkin/day.html`、`src/main/resources/templates/memo/index.html`、`src/main/resources/templates/user/profile.html`、`src/main/resources/static/css/app.css`、`docs/开发记录.md`
- 主要内容：新增统一的手机端底部模块导航，按首页、经营、打卡、资料、我的五个模块切换独立页面；同时把首页下半部分改为模块卡片总览，弱化单页长滚动感。
- 影响模块：前端样式、首页展示、经营模块、打卡模块、资料库、个人设置、全局手机端导航
- 验证结果：`mvn -q -DskipTests compile` 通过。
- 备注：未改动后端接口、数据库结构和页面路由。

### 2026-04-03 | Linux 启动配置修复
- 变更范围：`src/main/resources/application-prod.yml`、`docs/上线部署指南.md`、`docs/开发记录.md`
- 主要内容：将生产配置里的固定公网数据库地址改为“本机 MySQL 默认值 + 环境变量可覆盖”，同时把上传目录和日志文件路径也改成可覆盖，避免 Jar 发到其他 Linux 服务器后默认连错库导致启动失败。
- 影响模块：生产环境启动配置、上线部署文档
- 验证结果：`mvn -q -DskipTests compile` 通过；使用环境变量覆盖数据库与日志路径后，执行 `java -jar target/codexzy-1.0.0.jar --server.port=18080` 可成功启动并监听 `18080` 端口。
- 备注：新增支持 `CODEXZY_DB_URL`、`CODEXZY_DB_USERNAME`、`CODEXZY_DB_PASSWORD`、`CODEXZY_FILE_UPLOAD_DIR`、`CODEXZY_LOG_FILE`。

### 2026-04-03 | Linux 重启脚本
- 变更范围：`restart-codexzy.sh`、`docs/上线部署指南.md`、`docs/开发记录.md`
- 主要内容：新增可直接上传到 Linux 服务器使用的 Jar 启停脚本，默认支持同目录 `codexzy-1.0.0.jar`，并补充部署文档中的脚本用法说明。
- 影响模块：部署脚本、上线部署文档
- 验证结果：脚本语法已完成静态检查，未涉及后端编译。
- 备注：脚本支持 `start`、`stop`、`restart`、`status`，可通过环境变量覆盖 Jar 路径和 Java 路径。

### 2026-04-03 | 手机端分栏改版
- 变更范围：`src/main/resources/static/css/app.css`、`docs/开发记录.md`
- 主要内容：将手机端顶部操作区改为固定底部导航式入口，首页在小屏幕下收起辅助内容并压缩首屏按钮与卡片间距，减少单页纵向滚动。
- 影响模块：前端样式、首页移动端展示、全局移动端导航
- 验证结果：`mvn -q -DskipTests compile` 通过。
- 备注：未改动后端接口、数据库结构和页面路由。

### 2026-04-03 | 长文件拆分规则补充
- 变更范围：`AGENTS.md`、`docs/PROJECT_RULES.md`、`docs/开发记录.md`
- 主要内容：补充当单个文件明显过长且职责混杂时，可在不影响现有结构的前提下优先拆分为多个文件的规则，便于后续快速开发与维护。
- 影响模块：文档
- 验证结果：仅规则文档更新，未涉及代码编译。
- 备注：保持与项目规则文档一致。

### 2026-04-03 | 页面视觉收敛
- 变更范围：`src/main/resources/static/css/app.css`、`src/main/resources/templates/business/index.html`、`src/main/resources/templates/business/form.html`、`src/main/resources/templates/checkin/list.html`、`src/main/resources/templates/checkin/calendar.html`、`src/main/resources/templates/checkin/day.html`、`src/main/resources/templates/checkin/form.html`
- 主要内容：将经营后台、经营录入页和打卡相关页面的辅助说明改为默认折叠展示，筛选和统计类内容改成按需展开；同时把移动端顶部操作区调整为横向胶囊按钮，减少首屏拥挤感。
- 影响模块：前端样式、经营后台、经营录入页、打卡列表、月历视图、日期详情、打卡编辑页
- 验证结果：`mvn -q -DskipTests compile` 通过。
- 备注：未改动后端接口和数据库结构。

### 2026-04-03 | 经营页首屏精简
- 变更范围：`src/main/resources/templates/business/index.html`、`src/main/resources/static/css/app.css`、`docs/开发记录.md`
- 主要内容：精简经营页首屏文案，移除辅助说明句并把主标题缩短为更直接的入口表达，移动端首屏只保留更必要的信息。
- 影响模块：经营后台首页首屏展示
- 验证结果：已在后续执行 `mvn -q -DskipTests compile`，通过。
- 备注：保留报单和商品入库两个入口按钮，不改动接口和数据结构。

### 2026-04-03 | UTF-8 文档读取规则补充
- 变更范围：`AGENTS.md`、`docs/PROJECT_RULES.md`
- 主要内容：补充查看仓库文档时默认按 UTF-8 编码读取的规则，避免后续因编码不一致导致乱码。
- 影响模块：文档
- 验证结果：仅规则文档更新，未涉及代码变更。
- 备注：后续查看文档时优先使用 UTF-8。

### 2026-04-03 | 个人报码改造
- 变更范围：`src/main/resources/templates/index.html`、`src/main/resources/templates/business/index.html`、`src/main/resources/templates/business/form.html`、`src/main/resources/templates/user/profile.html`、`src/main/resources/static/js/code-copy.js`、`src/main/resources/static/css/app.css`
- 主要内容：将当前报码文案统一调整为个人报码，并在首页、经营后台、经营录入页和个人设置页增加一键复制操作。
- 影响模块：首页、经营后台、经营录入页、个人设置页、前端样式、前端脚本
- 验证结果：`mvn -q -DskipTests compile` 通过。
- 备注：个人报码仍然唯一绑定当前账号，复制按钮仅负责前端复制到剪贴板。

### 2026-04-03 | 报单状态归属修正
- 变更范围：`src/main/java/com/codexzy/service/impl/BusinessRecordServiceImpl.java`、`src/main/resources/templates/business/form.html`、`src/test/java/com/codexzy/service/impl/BusinessRecordServiceImplTest.java`
- 主要内容：报单创建时忽略报单人提交的状态值，统一按默认状态落库；同时在经营表单里补充说明，报单状态只能由上家修改。
- 影响模块：经营记录创建、表单提示、单元测试
- 验证结果：`mvn -q -DskipTests clean compile` 通过
- 备注：不影响入库记录和编辑后的状态修改流程。

### 2026-04-03 | JDK 路径规则补充
- 变更范围：`AGENTS.md`、`docs/PROJECT_RULES.md`
- 主要内容：补充本机可用的 Java 17 路径优先级，避免后续编译时重复查找 JDK。
- 影响模块：文档
- 验证结果：仅规则文档更新，无需代码编译。
- 备注：优先使用 `C:\Users\admin\.jdks\ms-17.0.18`，备用为 `C:\Program Files\JetBrains\IntelliJ IDEA 2023.1\jbr`。

### 2026-04-03 | 路径修复
- 变更范围：`src/main/java/com/codexzy/config/SecurityConfig.java`、`src/main/java/com/codexzy/controller/HomeController.java`、`docs/开发记录.md`
- 主要内容：补充 `/.well-known/appspecific/com.chrome.devtools.json` 的公开访问与空 JSON 响应，避免 Chrome DevTools 探测请求返回 404。
- 影响模块：安全配置、首页控制器、浏览器探测路径
- 验证结果：已使用 JBR 21 执行 `mvn -q -DskipTests compile`，通过。
- 备注：不影响现有登录和首页业务流程。

### 2026-04-03 | 启动修复
- 变更范围：`src/main/resources/schema.sql`、`docs/开发记录.md`
- 主要内容：将 `business_record.record_status` 的补列语句改为基于 `INFORMATION_SCHEMA` 的条件执行，避免 `ADD COLUMN IF NOT EXISTS` 在当前 MySQL 版本下报语法错误，并保持旧库兼容。
- 影响模块：数据库初始化、业务记录模块
- 验证结果：`mvn -q -DskipTests compile` 通过。
- 备注：无接口变更。

### 2026-04-03 | 文档初始化
- 变更范围：`docs/开发记录.md`、`AGENTS.md`
- 主要内容：新增开发记录文档，并补充每次开发或改动后必须同步维护文档的规则。
- 影响模块：文档
- 验证结果：仅文档变更，无需代码编译。
- 备注：后续每次功能开发、修复、配置调整都应继续追加记录。

### 2026-04-03 | 手机端优化
- 变更范围：`src/main/resources/static/css/app.css`、`docs/开发记录.md`
- 主要内容：将手机端的顶部操作区、提交区和个人报码复制区改成纵向布局，取消首页和经营相关辅助卡片的手机端隐藏，并把月历在常规手机宽度下调整为两列、在更窄屏幕下再收敛为单列。
- 影响模块：首页、打卡档案、日期详情、经营后台、经营录入、个人设置、月历视图
- 验证结果：`mvn -q -DskipTests clean compile` 通过。
- 备注：未改动接口和数据库结构。

### 2026-04-07 | 数据库连接修复
- 变更范围：`src/main/resources/application-prod.yml`、`docs/开发记录.md`
- 主要内容：修正生产环境 MySQL 连接串，补充 `allowPublicKeyRetrieval=true`，并移除末尾多余的 `}`，解决 MySQL 8 认证时的 `Public Key Retrieval is not allowed` 连接异常。
- 影响模块：数据源初始化、应用启动
- 验证结果：待执行 `mvn -q -DskipTests compile`
- 备注：如果数据库仍要求 SSL，可再根据实际环境调整连接参数。

### 2026-04-07 | 首页移动端导航收口
- 变更范围：`src/main/resources/templates/index.html`、`docs/开发记录.md`
- 主要内容：将首页顶部模块入口在手机端隐藏，改由底部固定导航承担模块切换，避免首页首屏同时出现两套导航导致操作路径重复。
- 影响模块：首页移动端布局
- 验证结果：`mvn -q -DskipTests compile` 通过。
- 备注：仅调整首页移动端展示，桌面端顶部入口保留，未改动接口和数据库结构。

### 2026-04-07 | 移动端布局收口
- 变更范围：`src/main/resources/static/css/app.css`、`src/main/resources/templates/business/index.html`、`src/main/resources/templates/business/form.html`、`src/main/resources/templates/business/reporter.html`、`src/main/resources/templates/checkin/list.html`、`src/main/resources/templates/memo/index.html`、`src/main/resources/templates/user/profile.html`、`docs/开发记录.md`
- 主要内容：收紧手机端头部布局，优化个人报码与复制按钮的排版；将纯模块跳转型顶部按钮在手机端隐藏，只保留当前页必要操作；同时把经营页统计卡片改为更紧凑的双列展示，减少首屏空白和重复导航带来的割裂感。
- 影响模块：经营后台、经营录入、报单详情、打卡列表、资料库、个人设置、全局手机端样式
- 验证结果：`mvn -q -DskipTests compile` 通过。
- 备注：未修改接口、数据库结构和权限逻辑。

### 2026-04-07 | 顶部胶囊导航移除
- 变更范围：`src/main/resources/templates/index.html`、`src/main/resources/templates/business/index.html`、`src/main/resources/templates/business/form.html`、`src/main/resources/templates/business/reporter.html`、`src/main/resources/templates/checkin/list.html`、`src/main/resources/templates/checkin/calendar.html`、`src/main/resources/templates/checkin/form.html`、`src/main/resources/templates/checkin/day.html`、`src/main/resources/templates/memo/index.html`、`src/main/resources/templates/user/profile.html`、`src/main/resources/static/css/app.css`、`docs/开发记录.md`
- 主要内容：删除各页面头部整排胶囊导航，避免页面顶部重复堆叠入口；将返回、新建、翻页等必要操作迁回各自内容区标题旁，退出登录统一放到个人设置页处理。
- 影响模块：首页、经营后台、经营录入、报单详情、打卡列表、打卡月历、打卡编辑、日期详情、资料库、个人设置、全局页面样式
- 验证结果：`mvn -q -DskipTests compile` 通过。
- 备注：未改动路由、接口和数据库结构。

### 2026-04-07 | 首页与经营页视觉层级收口
- 变更范围：`src/main/resources/static/css/app.css`、`docs/开发记录.md`
- 主要内容：统一首页与经营页的卡片层级、字体比例和配色关系，收紧 hero 区、首页模块卡片、经营统计卡片和报单人卡片的视觉重量，减少大块堆叠和颜色分散带来的割裂感。
- 影响模块：首页、经营后台、全局前端样式
- 验证结果：`mvn -q -DskipTests compile` 通过。
- 备注：仅调整样式，不改动接口、数据库和页面流程。

### 2026-04-07 | 首屏信息密度继续收紧
- 变更范围：`src/main/resources/templates/index.html`、`src/main/resources/templates/business/index.html`、`src/main/resources/static/css/app.css`、`docs/开发记录.md`
- 主要内容：继续压缩首页与经营页首屏文案长度，补强经营页 hero 的当前筛选摘要，并进一步弱化首页辅助区、强化模块卡片底部层级，让首屏更聚焦主入口和主操作。
- 影响模块：首页、经营后台、前端样式
- 验证结果：`mvn -q -DskipTests compile` 通过。
- 备注：未改动接口、数据库和页面路由。

### 2026-04-07 | 首页主次入口强化
- 变更范围：`src/main/resources/templates/index.html`、`src/main/resources/templates/business/index.html`、`src/main/resources/static/css/app.css`、`docs/开发记录.md`
- 主要内容：将首页四张模块卡片拆成主入口和次入口两档，强化经营与打卡的视觉权重；同时将经营页统计卡片拆成核心统计与金额统计两组，让首屏更快看出重点。
- 影响模块：首页、经营后台、前端样式
- 验证结果：`mvn -q -DskipTests compile` 通过。
- 备注：仅调整模板 class 与样式，不改动接口和数据结构。

### 2026-04-07 | 首页首屏结构重组
- 变更范围：`src/main/resources/templates/index.html`、`src/main/resources/static/css/app.css`、`docs/开发记录.md`
- 主要内容：将首页首屏重组为“主视觉 + 两个主入口 + 两个次入口”的一屏结构，把 hero 与经营/打卡主入口合并成统一首屏，减少原先首屏继续向下找主功能的割裂感。
- 影响模块：首页、前端样式
- 验证结果：`mvn -q -DskipTests compile` 通过。
- 备注：仅调整首页模板结构与样式，不改动接口和路由。

### 2026-04-07 | 页面标签中文统一
- 变更范围：`src/main/resources/templates/index.html`、`src/main/resources/templates/checkin/list.html`、`src/main/resources/templates/checkin/calendar.html`、`src/main/resources/templates/checkin/form.html`、`src/main/resources/templates/checkin/day.html`、`src/main/resources/templates/memo/index.html`、`src/main/resources/templates/user/profile.html`、`src/main/resources/templates/login.html`、`src/main/resources/templates/register.html`、`docs/开发记录.md`
- 主要内容：将首页、打卡、资料库、个人设置以及登录注册页中残留的英文分区标签统一替换为中文，收口页面语义和视觉语言，避免同一套界面中中英混用造成的割裂感。
- 影响模块：首页、打卡模块、资料库、个人设置、登录页、注册页
- 验证结果：`mvn -q -DskipTests compile` 通过。
- 备注：仅调整模板可见文案，不改动控制器、接口和数据库结构。

### 2026-04-07 | 页面文案进一步收口
- 变更范围：`src/main/resources/templates/index.html`、`src/main/resources/templates/business/index.html`、`src/main/resources/templates/business/form.html`、`src/main/resources/templates/business/reporter.html`、`src/main/resources/templates/checkin/list.html`、`src/main/resources/templates/checkin/calendar.html`、`src/main/resources/templates/memo/index.html`、`src/main/resources/templates/user/profile.html`、`src/main/resources/templates/login.html`、`src/main/resources/templates/register.html`、`src/main/java/com/codexzy/controller/BusinessController.java`、`src/main/java/com/codexzy/service/BusinessRecordService.java`、`src/main/java/com/codexzy/service/impl/BusinessRecordServiceImpl.java`、`docs/开发记录.md`
- 主要内容：继续压缩首页与经营相关页面中偏系统化的措辞，统一“去某页 / 继续某动作”这类按钮说法；同时把“经营后台 / 模块 / 视图 / 我可以继续做”这类不自然文案替换为更贴近日常使用场景的表达。编译过程中顺手清理了 3 个 Java 文件的 UTF-8 BOM，仅修复编码，不改动业务逻辑。
- 影响模块：首页、经营模块、打卡模块、资料库、个人设置、登录页、注册页
- 验证结果：`mvn -q -DskipTests compile` 通过。
- 备注：模板层只调整可见文案；3 个 Java 文件仅处理编码问题，不涉及控制器、服务实现和接口逻辑变更。

